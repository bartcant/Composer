/**
 * Access control rules for bart-network
 */
rule Default {
    description: "Allow all participants access to all resources"
    participant: "ANY"
    operation: ALL
    resource: "org.acme.bart-network.*"
    action: ALLOW
}
