/**
 * Access control rules for my-first-network
 */
rule Default {
    description: "Allow all participants access to all resources"
    participant: "ANY"
    operation: ALL
    resource: "org.acme.bartnetwork.*"
    action: ALLOW
}
